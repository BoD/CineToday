// Repositories used across modules
RepositoryHandler.metaClass.addRepos = {
    delegate.with {
        mavenLocal()
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}
